# commandes (dont on se sert) de base docker network

`docker network create NETWORKNAME`

`docker network rm NETWORKNAME`

`docker network connect RUNNINGCONTAINERNAME`

`docker network disconnect RUNNINGCONTAINERNAME`
